#include <bits/stdc++.h>

using namespace std;

int main() {
  int t, n, s, a; for (cin >> t; t-- and cin >> n; cout << s << '\n') {
    for (s = 0; n-- and cin >> a; s = a * (s / a + 1));
  }
}
//1932B